#include<stdio.h>
void swap(int a, int b);
int main()
{
    int x = 10, y = 20;
    swap(x, y);
    return 0;
}
void swap(int a, int b)
{
    a = a + b;
    b = a - b;
    a = a - b;

    printf("The swapped Numbers are x = %d and y = %d", a, b);
}
